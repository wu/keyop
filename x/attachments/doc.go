// Package attachments provides a web UI tab for uploading and managing file attachments.
// Files are stored on disk under a configurable upload directory, organised into
// per-date sub-directories (YYYY-MM-DD). Metadata is persisted in a local SQLite database.
package attachments
