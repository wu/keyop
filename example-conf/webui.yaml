x: webui

config:
  port: 8080



subs:

  idle:
    name: idle
    description: process incoming events

  tides:
    name: tides
    description: process incoming events

  alerts:
    name: alerts
    description: process incoming events

  messenger:
    name: messenger
    description: process incoming events

  astral:
    name: astral
    description: process incoming events

  aurora:
    name: aurora
    description: process incoming events

  temp-shared:
    name: temp-shared
    description: process incoming events

  status:
    name: status
    description: process incoming events

  status-shared:
    name: status-shared
    description: process incoming events

  status-ping:
    name: status-ping
    description: process incoming events


