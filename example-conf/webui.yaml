x: webui

config:
  port: 8080

  auth_enabled: true
  passkey_rp_id: "homelab.example.com"
  passkey_rp_origin: "https://homelab.example.com"


subs:

  idle:
    name: idle
    description: process incoming events

  tides:
    name: tides
    description: process incoming events

  alerts:
    name: alerts
    description: process incoming events

  messenger:
    name: messenger
    description: process incoming events

  astral:
    name: astral
    description: process incoming events

  aurora:
    name: aurora
    description: process incoming events

  temp:
    name: temp
    description: process incoming events

  status:
    name: status
    description: process incoming events

  status-ping:
    name: status-ping
    description: process incoming events

  heartbeat:
    name: heartbeat
    description: process incoming events

  tasks:
    name: tasks
    description: synchronize task state

