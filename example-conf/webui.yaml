x: webui

config:
  port: 8080
  dbPaths:
    core.alert.v1: sqlite/alerts.sql
    core.error.v1: sqlite/errors.sql
    core.temp.v1: sqlite/temps.sql

subs:

  idle:
    name: idle
    description: process incoming events

  tides:
    name: tides
    description: process incoming events

  alerts:
    name: alerts
    description: process incoming events

  errorevents:
    name: errorevents
    description: process incoming events

  temp:
    name: temp
    description: process incoming events

  messenger:
    name: messenger
    description: process incoming events


